name: atmoNet
channels:
  - defaults
  - conda-forge

dependencies:
  - python=3.7                                  # general
  - matplotlib=3
  - numpy
  - spyder=4.2.5                                # 5 still buggy
  - keras                                       # for deep learning
  - tensorflow                                  # for deep learning
  - beautifulsoup4                              # For LiCSBAS tools
  - statsmodels                                 # For LiCSBAS tools
  - gdal                                        # for LiCSBAS tools  
  - pip
  - pip:
    - wget
